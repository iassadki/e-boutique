{% include 'base.html.twig' %}

{# {% block title %}New CustomerAddress
{% endblock %} #}

{% block body %}
	<div class="container">
		{% if cart_lines is empty %}
			<div class="row justify-content-center">
				<div class="col-md-6 text-center">
					<h1>Vous ne pouvez pas commander, votre panier est vide</h1>
					<a href="{{ path('app_product_index') }}" class="btn btn-primary">Retour à la boutique</a>
				</div>
			</div>
		{% else %}
			<div class="row justify-content-center">
				<div class="col-md-6">
					<h1 class="text-center">Veuillez renseigner votre adresse</h1>
					{{ include('customer_address/_form.html.twig') }}
				</div>
			</div>
		{% endif %}
	</div>
{% endblock %}

{% include 'footer.html.twig' %}

